# Task 3 workspace

Implement EF Core configuration and seed data here. The classes under `Models/` are starter domain contracts. Services and authentication depend on stable DbSet names and navigation properties.
