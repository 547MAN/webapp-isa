# Task 4 controller workspace

Keep controllers thin: bind DTOs, identify the current user, call a service and translate the result into an HTTP response. Route contracts are consumed by `client/src/api/`.
